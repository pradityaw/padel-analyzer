## Guardrails (Padel Analyzer)

- Stack: React 19 + Vite + Express + tRPC + Drizzle + SQLite — **not** Python/Flask
- Branch: `main` unless this task says otherwise
- One backlog item per agent — keep the diff narrow
- Do not delete `client/`, `server/`, `shared/`, or `mobile/`
- Read `AGENTS.md` and stay in the assigned workstream
- Run `npm run typecheck` before opening a PR
- Do not commit or push unless explicitly asked
