{
  "branchName": "ralph/production-ready",
  "stories": [
    {"id": "1", "title": "Add DB schema field for analysis processing state (pending/processing/complete/failed) and quality warnings JSON column", "passes": true, "priority": 1},
    {"id": "2", "title": "Persist in-progress analysis state to DB so navigating away does not lose work", "passes": true, "priority": 2},
    {"id": "3", "title": "Wire createAnalysisInputSchema from shared/schema.ts into the analysis.create tRPC router for server-side validation", "passes": true, "priority": 3},
    {"id": "4", "title": "Deduplicate PHASE_ORDER constant - remove from ProCompare.tsx and import from shared config", "passes": true, "priority": 4},
    {"id": "5", "title": "Refactor Upload.tsx 9 useState hooks into a single useReducer", "passes": true, "priority": 5},
    {"id": "6", "title": "Move landmark data from inline TEXT blob in SQLite to write-once .json files on disk, store only file path in DB", "passes": true, "priority": 6},
    {"id": "7", "title": "Implement lazy-loading of landmark data - only fetch when replay player component mounts", "passes": true, "priority": 7},
    {"id": "8", "title": "Fix history filter pill colors - replace hardcoded color #000 with theme-aware contrast colors", "passes": true, "priority": 8},
    {"id": "9", "title": "Consolidate distance() utility - unify the 2D and 3D versions in swingAnalyzer.ts and shotClassifier.ts", "passes": true, "priority": 9},
    {"id": "10", "title": "Add vitest setup and unit tests for swingAnalyzer core functions", "passes": true, "priority": 10},
    {"id": "11", "title": "Add unit tests for shotClassifier prediction and label mapping", "passes": true, "priority": 11},
    {"id": "12", "title": "Add integration tests for all tRPC routers using supertest", "passes": true, "priority": 12},
    {"id": "13", "title": "Add pino structured logging on server side with request ID tracking", "passes": true, "priority": 13},
    {"id": "14", "title": "Add React error boundary with fallback UI on client side", "passes": true, "priority": 14},
    {"id": "15", "title": "Add session-based auth middleware with login/register flow", "passes": true, "priority": 15},
    {"id": "16", "title": "Add E2E smoke test with Playwright for upload-analyze-view flow", "passes": true, "priority": 16},
    {"id": "17", "title": "Add express-rate-limit middleware (10 req/min on upload, 30 req/min on API)", "passes": true, "priority": 17},
    {"id": "18", "title": "Add database indexes on analyses.createdAt, annotations.analysisId, proComparisons.playerAnalysisId", "passes": true, "priority": 18},
    {"id": "19", "title": "Add pagination to analysis.list (limit/offset, default 20) and exclude landmarksJson from list queries", "passes": true, "priority": 19},
    {"id": "20", "title": "Harden YouTube URL validation with strict regex and add file cleanup for failed downloads", "passes": true, "priority": 20},
    {"id": "21", "title": "Add .env support for PORT, DB_PATH, UPLOAD_DIR with dotenv", "passes": true, "priority": 21}
  ]
}
