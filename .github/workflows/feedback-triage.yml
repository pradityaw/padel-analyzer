# DISABLED — this repo uses Slack-only feedback triage.
# Active workflow: .github/workflows/feedback-triage-slack.yml
name: Feedback triage (Telegram — disabled)

on:
  workflow_dispatch: {}

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
<<<<<<< HEAD
      - name: Telegram triage disabled
        run: |
          echo "Telegram feedback triage is permanently disabled."
          echo "Use Actions → Feedback triage (Slack) instead."
          echo "See scripts/feedback-bot/README.md"
=======
      - uses: actions/checkout@v4

      - name: Check Telegram feedback secrets
        id: telegram-secrets
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_FEEDBACK_CHAT_ID: ${{ secrets.TELEGRAM_FEEDBACK_CHAT_ID }}
        run: |
          if [ -n "$TELEGRAM_BOT_TOKEN" ] && [ -n "$TELEGRAM_FEEDBACK_CHAT_ID" ]; then
            echo "enabled=true" >> "$GITHUB_OUTPUT"
          else
            echo "::notice::Skipping Telegram feedback triage because TELEGRAM_BOT_TOKEN or TELEGRAM_FEEDBACK_CHAT_ID is not configured."
            echo "enabled=false" >> "$GITHUB_OUTPUT"
          fi

      - uses: actions/setup-node@v4
        if: steps.telegram-secrets.outputs.enabled == 'true'
        with:
          node-version: "22"
          cache: npm

      - name: Install dependencies
        if: steps.telegram-secrets.outputs.enabled == 'true'
        run: npm ci

      - name: Run feedback triage
        if: steps.telegram-secrets.outputs.enabled == 'true'
        env:
          CURSOR_API_KEY: ${{ secrets.CURSOR_API_KEY }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_FEEDBACK_CHAT_ID: ${{ secrets.TELEGRAM_FEEDBACK_CHAT_ID }}
          TELEGRAM_ALLOWLIST_USER_IDS: ${{ secrets.TELEGRAM_ALLOWLIST_USER_IDS }}
          FEEDBACK_REPO_URL: ${{ secrets.FEEDBACK_REPO_URL }}
          FEEDBACK_MODEL: ${{ secrets.FEEDBACK_MODEL }}
          FEEDBACK_MAX_MESSAGES_PER_RUN: ${{ secrets.FEEDBACK_MAX_MESSAGES_PER_RUN }}
          FEEDBACK_MAX_PRS_PER_RUN: ${{ secrets.FEEDBACK_MAX_PRS_PER_RUN }}
        run: npm run feedback:triage
>>>>>>> f1a68bd (fix(feedback): skip unavailable triage automation)
