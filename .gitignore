node_modules/
dist/
data/*.db
data/uploads/
data/thumbnails/
data/reports/
data/landmarks/
data/analysis-agents/

# Python bytecode
**/__pycache__/
*.pyc

# Local env / OS noise
.env
.env.*
.env.feedback
!.env.example
!.env*.example
*.log
.DS_Store

data/*.db-wal
data/*.db-shm

# Agent / IDE artifacts (keep team rules in .cursor/rules if desired — local junk only)
.claude/
.cursor-sdk-runs/

# Cursor local agent state (keep shared rules in repo)
.cursor/agents/

# QA run output
qa-artifacts/

training/checkpoints/
training/data/*.json
training/data/**/*.json

# Optional: ignore ONNX in repo (uncomment if you distribute models separately)
# client/public/models/*.onnx

playwright-report/
test-results/
blob-report/
